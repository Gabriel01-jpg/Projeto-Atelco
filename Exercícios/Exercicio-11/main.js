/* 11. Elaborar um algoritmo que lê 2 valores a e b e os escreve com a mensagem: "São 
múltiplos" ou "Não são múltiplos". */

let A = 9;

let B = 5;

if(A % B == 0){ // SE O RESTO DA DIVISAO DOS NUMEROS FOR IGUAL A 0, SIGNIFICA QUE ELES SÃO MULTIPLOS
    console.log('São multiplos')
} else {
    console.log('Não são multiplos')
}
